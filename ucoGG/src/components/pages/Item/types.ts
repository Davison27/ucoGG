export type SpellProps = {
    description: string
    id: string
    name: string
    cooldown: string
    cost: string
};

export type SkinsProps = {
    num: string
    name: string
    chromas: boolean
}